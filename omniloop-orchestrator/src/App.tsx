import{useState}from"react";import{generateScript,generateSurge}from"./generator";import type{OmniConfig,ProxyType}from"./types";import"./index.css";const initial:OmniConfig={proxyType:"socks5",proxyServer:"",proxyPort:1080,enableAdblock:true,enableSideStoreCompatibility:true};export default function App(){const[c,setC]=useState(initial);const[o,setO]=useState({conf:"",js:""});const valid=!!c.proxyServer.trim()&&c.proxyPort>0&&c.proxyPort<65536;const u=<K extends keyof OmniConfig>(k:K,v:OmniConfig[K])=>setC(x=>({...x,[k]:v}));const gen=()=>valid&&setO({conf:generateSurge(c),js:generateScript(c)});const copy=(v:string)=>navigator.clipboard?.writeText(v);const dl=(n:string,v:string)=>{const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([v],{type:"text/plain"}));a.download=n;a.click();URL.revokeObjectURL(a.href)};return <main><header><h1>OmniLoop Orchestrator</h1><p>ตัวสร้าง configuration สำหรับ proxy/routing โดยไม่แตะ Network Extension ของ iOS</p></header><div className="warning"><b>ข้อจำกัด:</b> เว็บนี้ไม่สามารถสร้าง UDP loopback หรือ Packet Tunnel ระดับระบบได้ กฎ 127.0.0.1 เป็นเพียง routing rule</div><section className="card"><h2>Proxy</h2><div className="grid"><label>Type<select value={c.proxyType} onChange={e=>u("proxyType",e.target.value as ProxyType)}><option value="socks5">SOCKS5</option><option value="https">HTTPS</option><option value="http">HTTP</option></select></label><label>Server<input value={c.proxyServer} onChange={e=>u("proxyServer",e.target.value)} placeholder="proxy.example.com"/></label><label>Port<input type="number" min="1" max="65535" value={c.proxyPort} onChange={e=>u("proxyPort",Number(e.target.value))}/></label></div><label className="check"><input type="checkbox" checked={c.enableAdblock} onChange={e=>u("enableAdblock",e.target.checked)}/> AdBlock rules</label><label className="check"><input type="checkbox" checked={c.enableSideStoreCompatibility} onChange={e=>u("enableSideStoreCompatibility",e.target.checked)}/> SideStore compatibility routing</label><button disabled={!valid} onClick={gen}>Generate configuration</button></section><section className="outputs"><article className="card"><div className="row"><h2>Surge .conf</h2><span><button onClick={()=>copy(o.conf)}>Copy</button><button onClick={()=>dl("omniloop.conf",o.conf)}>Download</button></span></div><textarea value={o.conf} readOnly placeholder="กด Generate configuration"/></article><article className="card"><div className="row"><h2>Helper .js</h2><span><button onClick={()=>copy(o.js)}>Copy</button><button onClick={()=>dl("sidestore-helper.js",o.js)}>Download</button></span></div><textarea value={o.js} readOnly placeholder="กด Generate configuration"/></article></section></main>}